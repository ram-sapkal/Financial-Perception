# Financial Perception Frontend

This is the frontend client for the Financial Perception monorepo, built with React and Vite.

## Setup
1. Run `npm install`
2. Run `npm run dev` to start the development server.
